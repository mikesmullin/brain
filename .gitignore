# dependencies
node_modules/

# pglite data dir (rebuildable artifact — never committed; regenerated from .md on reindex)
.brain/pgdata/

# scratch
*.log
tmp/

# brain server runtime state (per-db singleton guard + unix socket)
db/pgdata/
db/.lock
db/.sock
